package com.nyotek.dot.admin.common

/**
 * Class that contains enums that used across modules
 */

/**
 * The enum class that representing the location types
 */
enum class NSLocationType(val value: Int) {
    TYPE_ACCURATE_LOCATION(1)
}

/**
 * The enum class that representing the language types
 */
enum class NSLanguage(val value: Int) {
    AR(1),
    EN(2),
    DE(3),
    FR(4)
}